jQuery(document).ready(function($){
	$('.add_to_cart_button').click(function(){
    var attribute = $(this).attr('data-product_id');
		
		/*$('.primary').append(load(localhost/testWith/panier/)).hide().remove($('.cart_item'));*/
  })

});
